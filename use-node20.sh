#!/bin/bash
# اسکریپت برای استفاده از Node.js v20 فقط در این پروژه
# این اسکریپت هیچ تغییری در Node.js سرور یا default nvm ایجاد نمی‌کند

# بارگذاری nvm
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

# بررسی اینکه Node.js 20.19.0 نصب شده یا نه
if ! nvm list | grep -q "20.19.0"; then
    echo "📦 در حال نصب Node.js v20.19.0 (فقط برای این پروژه)..."
    nvm install 20.19.0
fi

# استفاده از Node.js v20.19.0 (فقط در این shell)
nvm use 20.19.0

echo "✅ Node.js v20.19.0 فعال شد (فقط در این terminal)"
echo "💡 برای استفاده در terminal دیگر، این اسکریپت را دوباره اجرا کنید"
echo "📌 Node.js سرور (v18.18.2) بدون تغییر باقی می‌ماند"


